#include <stdio.h>
#include <unistd.h>
/**
 * main - entry point
 * Description: prints all possible combinations of single-digit numbers.
 * Return: 0
 */
int main(void)
{
	int i;

	for (i = 48; i < 58;i++)
	{
		_putchar(n);
		if (i != 57)
		{
			_putchar(',');
			_putchar(' ');
		}
	}
	_putchar('\n');
	return (0);
}
