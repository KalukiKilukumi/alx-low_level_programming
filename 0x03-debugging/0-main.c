#include "main.h"

/**
 * main - determine whether a number is negative or positive
 * i: number to be checked
 * Return:0 on success
 */

int main(void)
{
int i;

i = 0;
positive_or_negative(i);

return (0);
}
